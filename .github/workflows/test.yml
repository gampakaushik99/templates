name: first flow
#on: create
jobs:
    Build:
      runs-on: ubuntu-latest
      steps:
          - run: |
                   echo "hello"
                   echo "this is triggered when you create a tag"
